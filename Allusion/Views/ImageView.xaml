<controls:ImageControl x:Class="Allusion.Views.ImageView"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:controls="clr-namespace:Allusion.Controls"
                       xmlns:viewModels="clr-namespace:Allusion.ViewModels"
                       d:DataContext="{d:DesignInstance Type=viewModels:ImageViewModel}"
                       mc:Ignorable="d" IsSelected="{Binding Selected}">
<Border Background="Transparent" BorderThickness="10" BorderBrush="DarkOrange">
    <Image x:Name="BoundImage" Source="{Binding ImageSource}" d:DesignHeight="100" d:DesignWidth="100" Stretch="Uniform"/>
</Border>
</controls:ImageControl>