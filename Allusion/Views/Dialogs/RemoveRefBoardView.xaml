<Window
    x:Class="Allusion.Views.Dialogs.RemoveRefBoardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cm="http://caliburnmicro.com"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dialogs="clr-namespace:Allusion.ViewModels.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Title}"
    Width="400"
    Height="180"
    cm:Message.Attach="[Key Escape] = [Cancel]; [Key Enter] = [Remove]"
    d:DataContext="{d:DesignInstance Type=dialogs:RemoveRefBoardViewModel}"
    ResizeMode="NoResize"
    Style="{DynamicResource DialogWindow}"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="{x:Static SystemParameters.WindowResizeBorderThickness}" />
    </WindowChrome.WindowChrome>

    <Grid Style="{DynamicResource DialogContentGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Center"
            Content="{Binding Title}"
            Style="{DynamicResource DialogTitleLabel}" />
        <TextBlock
            Grid.Row="1"
            Style="{DynamicResource DialogMessageTextBlock}"
            Text="{Binding Message}" />
        <CheckBox
            Grid.Row="2"
            Content="Delete local files"
            IsChecked="{Binding DeleteLocalFiles}"
            Style="{DynamicResource DialogCheckBox}" />

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button
                x:Name="Remove"
                Grid.Column="2"
                Content="Remove"
                Style="{DynamicResource PrimaryButton}" />
            <Button
                x:Name="Cancel"
                Grid.Column="3"
                Content="Cancel"
                Style="{DynamicResource SecondaryButton}" />
        </Grid>
    </Grid>
</Window>
