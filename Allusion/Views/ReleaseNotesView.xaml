<UserControl
    x:Class="Allusion.Views.ReleaseNotesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="800"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="10,4"
            HorizontalAlignment="Center"
            FontSize="12"
            Foreground="{DynamicResource Foreground}"
            Text="{Binding Version}" />
        <ScrollViewer
            Grid.Row="1"
            Padding="10,0"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ReleaseHistory}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,4,0,10">
                            <TextBlock
                                Margin="0,2"
                                FontSize="16"
                                FontWeight="Bold"
                                Foreground="{DynamicResource Foreground.Front}"
                                Text="{Binding Header, Mode=OneWay}" />
                            <ItemsControl ItemsSource="{Binding Notes}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            Margin="0,6"
                                            FontSize="14"
                                            Foreground="{DynamicResource Foreground.Front}"
                                            TextWrapping="Wrap"><Run Text="• " /><Run Text="{Binding Mode=OneWay}" /></TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
